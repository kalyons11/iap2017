#include <stdio.h>

int main() {
	// int arr[10];
	// int arr[] = [10, 5, 6, 7];

	/*

	printf("Hello world. The number is %d!\n", 10);

	for (int i = 0; i < 10; i++) {
		printf("The current value of the array is %d.\n", i);
	}

	// Testing scan f.

	printf("Enter two integers. i.e. \"10 20\".\n");

	int i;
	int j;

	scanf("%d %d", &i, &j);

	printf("The first number you entered is %d.\n", i);

	printf("The second number you entered is %d.\n", j);

	

	// POINTERS

	double d = 1.5;
	double* x = &d;

	printf("%f\n", d);
	printf("%p\n", x);

	// Print the value.

	printf("%f\n", *x);

	

	// Pointer arith.

	char* s = "Hello world.";

	printf("%c\n", *s);
	printf("%c\n", s[0]);
	printf("%c\n", *(s + 1));
	printf("%c\n", s[1]);

	

	// Array practice.

	int count;

	printf("How many integer array objects do you want? ");

	scanf("%d", &count);

	while (count < 1) {
		printf("Come on, at least 1!!! Try again. ");
		scanf("%d", &count);
	}

	int arr[count];

	for (int i = 0; i < count; i++) {
		int new;
		printf("Value %d: ", i);
		scanf("%d", &new);
		arr[i] = new;
	}

	printf("Now, let's print the array.\n");

	for (int i = 0; i < count; i++) {
		printf("%d\n", arr[i]);
	}

	*/

	printf("Process complete.\n");

	return 0;
}